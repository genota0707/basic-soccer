# ホームページの日程更新

編集用スプレッドシートには「開催日程」と「予約空き枠」の2つのタブがあります。1行が1開催日、または1予約枠です。日付は `2026-10-24` の形式で入力し、「表示」を「公開」にするとサイトへ反映されます。「非公開」の行は表示しません。空き枠の「状態」は「空きあり」「満席」「受付終了」から選びます。空欄の時間や会場はサイトにも表示しません。過去の日付はサイト側で自動的に隠れます。

反映を有効にするには、オーナーが次を設定してください。

1. **公開する情報だけ**が表に入っていることを確認します。Google Sheetsの「ファイル → 共有 → ウェブに公開」で、2つのタブをそれぞれCSVとして公開します。公開CSVにはそのタブの内容がすべて含まれるので、電話番号や非公開の個人情報を入力しないでください。「非公開」はサイトへの表示だけを制御します。
2. GitHubリポジトリの Settings → Secrets and variables → Actions → Variables に `BASIC_EVENTS_CSV_URL`（開催日程の公開CSV URL）と `BASIC_BOOKINGS_CSV_URL`（予約空き枠の公開CSV URL）を登録します。
3. リポジトリの Settings → Pages → Build and deployment → Source を「GitHub Actions」に変更します。Actions → Deploy BASIC site and schedule → Run workflow で初回実行し、公開サイトの表示を確認します。以降は毎時17分頃に表を読み取り、サイト全体を再公開します。GitHub Actionsのスケジュール実行は遅れることがあります。GitHub上でサイトのコードを変更したときも自動で再公開します。

CSVが取得できない、列名や日付に誤りがある場合、更新ジョブは失敗し、公開中のサイトは変更しません。サイトの予約状況はリアルタイム在庫ではないため、確定前に公式LINEで確認してください。
