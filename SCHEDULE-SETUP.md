# ホームページの日程更新

編集用の[Googleスプレッドシート](https://docs.google.com/spreadsheets/d/1LKe_tTYD-peOqqbJA8r8IOedDM5Fn8NesN4VNE_ZwVI/edit)で「開催日程」と「予約空き枠」を管理します。接続済みの自動更新が毎時表を確認し、変更があるときだけ `data/schedule.json` を更新します。スプレッドシート自体をウェブ公開する必要はありません。

開催日程は1行が1開催日、予約空き枠は1行が1枠です。日付は `2026-10-24` のように入力してください。「表示」が「公開」の行だけサイトに掲載され、「非公開」は掲載されません。空き枠の「状態」は「空きあり」「満席」「受付終了」から選びます。過去の日付はサイト側で自動的に隠れます。

サイト用データは公開されます。氏名・電話番号などの個人情報は「公開」の行に入力しないでください。表の読み取りや更新に失敗した場合、公開中のサイトのデータは変更されません。予約の確定前には公式LINEで空き状況を確認してください。
